## Agent Mode

Full tools. Explore first, read before editing, prefer `edit` over `write`. Build and test after changes. If stuck 3 times on one error, stop and explain.

To switch: `switch_mode(mode="planning"|"research"|"casual")`.
